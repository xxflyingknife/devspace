# Frontend Environment Variables
# This file can be committed if it contains only non-sensitive defaults.

REACT_APP_API_BASE_URL=http://localhost:5001/api
NODE_ENV=development

# For production builds, create a .env.production file or set these in your build environment:
# REACT_APP_API_BASE_URL=/api  # Example for production if served under same domain
